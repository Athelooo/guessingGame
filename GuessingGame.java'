public class GuessingGame {
    public static void main(String[] args) {
        int low = 0;
        int high = 1000;  // vi kan ändra till 10 senare för snabbtest
        Guesser g = new Guesser(low, high);
        // g.start(); // lägger vi till i nästa steg
        System.out.println("GuessingGame compiled OK. Constructor works.");
    }
}
